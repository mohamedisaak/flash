# pnpm reads build-script permissions here. sharp ships prebuilt binaries; its
# install script enables Next.js image optimization in `next dev`.
allowBuilds:
  sharp: true
  # ESLint's import resolver ships this native binding, but our lint runs fine
  # without building it, so we explicitly decline the build script.
  unrs-resolver: false
